// Dedicated research-build capability surface. A research build must replace
// this object explicitly; no capability is inherited from the main Helix UI.
export const NEUROSLEEP_RESEARCH_FLAGS = Object.freeze({
  import_v1: false,
  shadow_v1: false,
  research_ui_v1: false,
  rvf_v1: false,
});
