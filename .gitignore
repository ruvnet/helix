# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Node / tooling (for when the harness is minted into this repo)
node_modules/
dist/
build/
.env
.env.*
*.log

# Rust (RuVector / Ruflo crates)
target/
**/*.rs.bk

# Local vault / health data must never be committed
*.vault
vault/
*.fhir.json
data/
*.vcf
*.fastq

# Stray tooling artifacts
.claude-flow/
ruvector.db
*.db

# ═══════════════════════════════════════════════════════════════════
# PRIVATE HEALTH DATA — NEVER COMMIT. THIS IS A PUBLIC REPO.
# Real PHI: Apple Health exports, clinical & pharmacy records,
# derived intelligence, personal vector stores. Best practice is to
# keep this OUTSIDE the repo entirely (see docs/Helix-Build-Spec.md).
# ═══════════════════════════════════════════════════════════════════
/Stu Health Data/
Stu Health Data/
**/Stu Health Data/
# Defense-in-depth: PHI file types anywhere in the working tree
Health Records*.json
HealthAutoExport*.zip
*_apple_health_export.json
*immunization*.pdf
prescriptions*.pdf
claw_health_*.json
claw_health_*.md
claw_swarm_memory.db*
ros3-agentdb.db
# Convention for any future private data — put it under private/
/private/
# Real (PHI) dossier drop-in for the local UI — never commit; only the
# synthetic demo-dossier.json is tracked. hybrid.html auto-loads this if present.
/ui/private/
ui/private/
